{
  "name": "practispace-learn",
  "version": "1.0.0",
  "private": true,
  "description": "Working with ACC — learning units for psychologists delivering ACC-funded treatment.",
  "scripts": {
    "build": "node build.js",
    "cards": "node tools/social-cards.js",
    "serve": "node build.js && cd dist && python3 -m http.server 8080"
  },
  "engines": { "node": ">=18" }
}
